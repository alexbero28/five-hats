// kit-version.mjs — the ONE place this kit's version is written down.
//
// The version exists because this kit is updated monthly and its output travels: a results page
// gets printed and handed to someone, a manifest sits on a stranger's disk for a year, and when
// either comes back with a question, "which kit produced this?" must be answerable from the
// artifact itself. A number in two files is a number that will disagree — same rule as the
// registry: one authority per fact.
//
// Bump it when behaviour changes, not when a comment does.
export const KIT_VERSION = '0.2.0';
